1、设计测试用例
2、写接口的测试用例case
3、写用例的py文件
    a、导入所需要的模块

4、Handle_context.py
第一步：
正则表达式区域,编写要替换的pattern
第二步：
写类方法，传入data，如果查到到要替换的pattern，就将要替换的值赋值，然后sub将实际值和pattern替换
第三步：
写个类方法，将所有要替换的字段分别调用第二步写好的类
一个excel里有3个字段要替换，那么第二步，就有三个类方法，在第三步里写的类方法里调用第二步的三个方法

第四步：
在cases的py里调用对应data在第三步的类方法